'use client';
import {useMemo,useState} from 'react';
import Link from 'next/link';
import {useRouter} from 'next/navigation';
import {Board} from './board';
import {PlanOperation} from '@/components/plan-operation';
import {fmtDate,fmtHeure} from '@/lib/utils';
export function PlanningWorkspace(p:any){
 const router=useRouter();
 const [mode,setMode]=useState<'guide'|'calendar'>('guide'),[tab,setTab]=useState('attente'),[search,setSearch]=useState(''),[selected,setSelected]=useState<string|null>(null),[message,setMessage]=useState('');
 const waiting=p.aPlanifier as any[];
 const scheduled=(p.ops as any[]).filter(o=>o.etat!=='a_planifier');
 const rows=(tab==='attente'?waiting:scheduled).filter(o=>[o.libelle,o.demande?.client?.nom,o.demande?.numero,o.adresse].join(' ').toLowerCase().includes(search.toLowerCase()));
 const all=useMemo(()=>Array.from(new Map([...p.ops,...waiting].map((o:any)=>[o.id,o])).values()) as any[],[p.ops,p.aPlanifier]);const op=all.find(o=>o.id===selected);
 return <div><header className="px-4 md:px-8 pt-6 pb-4"><div className="flex justify-between gap-3 flex-wrap"><div><p className="text-xs tracking-widest uppercase text-mute">Exploitation</p><h1 className="text-2xl font-semibold">Préparer les missions</h1><p className="text-sm text-mute mt-1">Une mission, un horaire, un camion, une équipe. Puis l’envoi au terrain.</p></div><div className="flex items-center gap-2 flex-wrap"><label className="text-sm">Date <input type="date" value={p.date} onChange={e=>{if(e.target.value)router.push(`/planning?date=${e.target.value}&vue=jour`);}} className="border border-line p-2 rounded-lg"/></label><button onClick={()=>setMode('guide')} className={`rounded-lg px-4 py-3 text-sm ${mode==='guide'?'bg-ink text-white':'border border-line bg-paper'}`}>Planifier pas à pas</button><button onClick={()=>setMode('calendar')} className={`rounded-lg px-4 py-3 text-sm ${mode==='calendar'?'bg-ink text-white':'border border-line bg-paper'}`}>Voir le calendrier</button></div></div></header>{mode==='calendar'?<Board {...p} canEdit={false}/>:<div className="px-4 md:px-8 pb-8 space-y-4"><div className="grid sm:grid-cols-3 gap-3"><button onClick={()=>{setTab('attente');setSelected(null);}} className="bg-ochre-soft rounded-xl text-left p-4"><strong className="text-2xl">{waiting.length}</strong><p className="text-sm">opérations à affecter · toutes dates</p></button><button onClick={()=>{setTab('planifiees');setSelected(null);}} className="bg-moss-soft rounded-xl text-left p-4"><strong className="text-2xl">{scheduled.length}</strong><p className="text-sm">missions affectées · {fmtDate(p.from)}{p.to!==p.from?` → ${fmtDate(p.to)}`:''}</p></button><Link href="/demandes?f=a_traiter" className="rounded-xl border border-line bg-paper p-4"><strong>Demandes à accepter →</strong><p className="text-sm text-mute mt-2">Une demande doit être acceptée avant l’affectation.</p></Link></div>{message && <p role="status" className="bg-moss-soft p-3 rounded-lg">{message}</p>}<div className="grid lg:grid-cols-[340px_1fr] gap-4"><section className="rounded-xl border border-line bg-paper p-4"><div className="flex gap-2 mb-3"><button onClick={()=>setTab('attente')} className={`px-3 py-2 rounded-lg text-sm ${tab==='attente'?'bg-ink text-white':'bg-fog'}`}>À affecter</button><button onClick={()=>setTab('planifiees')} className={`px-3 py-2 rounded-lg text-sm ${tab!=='attente'?'bg-ink text-white':'bg-fog'}`}>Déjà affectées</button></div><input aria-label="Rechercher une mission" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Client, lieu, numéro…" className="w-full border border-line rounded-lg p-3 text-sm mb-3"/><div className="space-y-2 max-h-[75vh] overflow-auto">{rows.length===0 && <p className="text-sm text-mute py-6">Aucune mission dans cette liste.</p>}{rows.map(o=><button key={o.id} onClick={()=>{setSelected(o.id);setMessage('');}} className={`block w-full text-left border rounded-xl p-3 ${selected===o.id?'border-cobalt bg-cobalt-soft':'border-line hover:bg-fog'}`}><p className="font-semibold">{o.demande?.client?.nom??'Client à préciser'}</p><p className="text-sm mt-1">{o.libelle??o.type_operation}</p><p className="text-xs text-mute mt-1">{fmtDate(o.date_prevue)} · {fmtHeure(o.heure_debut)} · {o.demande?.numero}</p><p className="text-xs mt-2 whitespace-pre-line">{o.adresse||'Lieu à préciser'}</p><span className="inline-block text-sm text-cobalt-ink mt-3">{o.etat==='a_planifier'?'Affecter les moyens →':'Consulter / ajuster →'}</span></button>)}</div></section><section className="rounded-xl border border-line bg-paper p-5 md:p-6">{op?<><div className="mb-5 pb-4 border-b border-line"><h2 className="text-xl font-semibold">{op.libelle}</h2><p>{op.demande?.client?.nom} · {op.demande?.numero}</p><Link href={`/demandes/${op.demande_id}`} className="text-sm text-cobalt-ink">Dossier et calcul d’itinéraire →</Link></div>{p.canEdit && ['a_planifier','planifiee'].includes(op.etat)?<PlanOperation key={`${op.id}-${op.updated_at}`} op={op} camions={p.camions} equipiers={p.equipiers} need={op.demande?.nb_hommes??1} onDone={()=>{setSelected(null);setMessage('Planning enregistré. La mission affectée apparaît dans la vue des équipiers rattachés à un compte.');}}/>:<p className="text-sm text-mute">{p.canEdit?'Mission en cours ou clôturée. Consultez le dossier pour suivre son exécution.':'Lecture seule : le responsable planning affecte les moyens.'}</p>}</>:<div className="py-16 text-center"><h2 className="text-xl font-semibold">Quelle mission préparez-vous ?</h2><p className="text-mute mt-3 max-w-md mx-auto">Choisissez une mission dans la liste. Vous pourrez définir le lieu, l’horaire, le camion et les personnes qui interviennent.</p></div>}</section></div></div>}</div>;
}
